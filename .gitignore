build/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
.graph/sessions.db
# --- copilot-sync repsol ---
.env
.env.*
!.env.example
!.env.sample
!.env.template
# --- /copilot-sync repsol ---
